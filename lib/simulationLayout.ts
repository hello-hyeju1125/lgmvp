/**
 * 시뮬레이션 HUD(스테퍼·KPI)·메인 본문·하단 푸터 가로 정렬 공통값.
 * 바깥 패딩 + 안쪽 max-w-7xl(mx-auto)로 위아래 좌우 끝선 일치.
 */
export const SIM_COLUMN_GUTTER = "px-4 sm:px-8";
export const SIM_COLUMN_MAX_INNER = "mx-auto w-full max-w-7xl";
